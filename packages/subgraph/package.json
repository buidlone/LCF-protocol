{
  "name": "@buidlone/subgraph",
  "version": "0.0.1",
  "description": "Subgraph for investment and governance pools",
  "main": "index.js",
  "scripts": {
    "abi": "node scripts/getAbi.js",
    "codegen": "graph codegen",
    "build": "graph build",
    "test": "graph test",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ investment-pool",
    "create-local": "graph create --node http://localhost:8020/ investment-pool",
    "remove-local": "graph remove --node http://localhost:8020/ investment-pool",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 investment-pool"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@graphprotocol/graph-ts": "^0.29.1"
  }
}
