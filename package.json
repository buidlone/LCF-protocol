{
    "name": "@buidlone/contracts",
    "version": "0.0.1",
    "private": true,
    "description": "Buidl1 contracts monorepo.",
    "scripts": {
        "build": "yarn workspaces run build",
        "build:contracts":"set -e;for i in investment-pool governance-pool distribution-pool;do yarn workspace @buidlone/$i build;done"
    },
    "workspaces": {
        "packages": [
            "packages/*"
        ]
    }
}
